#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a = 15,
        b = 20;
    if (a > b)
    {
        printf("%d est plus grand que %d\n", a, b); /* code */
    }
    else
    {
        printf("%d est plus grand que %d\n", b, a); /* code */
    }

    return 0;
}
